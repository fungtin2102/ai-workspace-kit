# Module Cline Rules - <MODULE_NAME>

The current directory is the `<MODULE_NAME>` module.

Read first:
- `AGENTS.md`
- `.clinerules/memory-bank-protocol.md`

If the parent root project exists, prefer:
- `../AGENTS.md`
- `../memory-bank/`
- `../dev-doc/`

If the parent root project does not exist, ask the user for the root project path or ask them to add the root project to the workspace. Do not create a second `memory-bank/` inside the module.

Edit only this module by default. Write cross-module changes to root contract documents first.
