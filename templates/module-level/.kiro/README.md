# Kiro Notes - <MODULE_NAME>

When Kiro works in this module:
- Read `AGENTS.md` first.
- Then read `.clinerules/` and `.cursor/rules/`.
- Prefer root `../memory-bank/`.
- If this is a standalone clone, ask for the root project path or ask the user to add the root project to the workspace.
- Edit only this module by default.
