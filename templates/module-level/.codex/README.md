# Codex Notes - <MODULE_NAME>

Codex primarily reads `AGENTS.md`. Use this directory only for lightweight module-level Codex notes if needed.

Recommendations:
- Treat `AGENTS.md` as the source of behavior rules.
- Write long-term state to the root `memory-bank/`.
- Write cross-module contracts to the root `dev-doc/`.
- Do not maintain a second module-level `memory-bank/`.
